// Placeholder for local `npm run dev`/`vite preview`. In the Docker image
// this file is regenerated at container start from the API_URL env var
// (see web/Dockerfile + web/docker-entrypoint.sh) so the same built image
// can point at any backend host without a rebuild.
window.__ALARMLOCK_API_URL__ = undefined;
